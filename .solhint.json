{
  "extends": "solhint:recommended",
  "rules": {
    "compiler-version": ["error", "^0.4.11"],
    "func-visibility": ["error", { "ignoreConstructors": true }],
    "max-line-length": ["warn", 120],
    "no-empty-blocks": "warn",
    "avoid-tx-origin": "error",
    "no-global-import": "error"
  }
}
